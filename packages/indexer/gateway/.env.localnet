# PostGraphile reads DATABASE_URL (fallback for -c) and PORT natively.
# Points at the squid_archive DB the chainflip-backend localnet populates on :5432.
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/squid_archive
PORT=8000
